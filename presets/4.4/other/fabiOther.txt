#$ TITLE: Defaults Fabi - apply without risk
#$ FIRMWARE_VERSION: 4.4
#$ CATEGORY: OTHER
#$ STATUS: COMMUNITY
#$ KEYWORDS: fabi
#$ AUTHOR: Fabian Eschle

#$ PARSER: MARKED

#$ DESCRIPTION: Fabi defaults - for all quads

# -- Start Fabi --
# feature
feature -AIRMODE
feature GPS
feature OSD
feature ESC_SENSOR
feature TELEMETRY

# beeper
beeper -DISARMING
beeper -ARMING
beeper -BAT_LOW
beeper -SYSTEM_INIT
beeper -ON_USB

# beacon
beacon RX_LOST
beacon RX_SET


# master
set min_check = 1005
set max_check = 1995

set bat_capacity = 800

set yaw_motors_reversed = ON
set small_angle = 180

#$ OPTION BEGIN (CHECKED): GPS Settings
set gps_sbas_mode = EGNOS
set gps_ublox_use_galileo = ON
set gps_set_home_point_once = ON
set gps_rescue_allow_arming_without_fix = ON

set gps_rescue_min_sats = 6
set gps_rescue_min_start_dist = 15

#$ OPTION END

set vbat_sag_compensation = 100

set debug_mode = GYRO_SCALED

# -- End Fabi --